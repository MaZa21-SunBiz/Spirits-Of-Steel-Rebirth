shader_type canvas_item;

void fragment() {
	//vec2 uv = (2.*SCREEN_UV-1.)*vec2(2., 1.);
	//COLOR.b = 0.;
	//vec2 ball = (2.*UV-1.)/vec2(1., 2.)*(2.-(sqrt((1.-pow(uv.x, 2))*(1.-pow(uv.y, 2)))));
	//COLOR.rg = (ball+vec2(1., 1.))*.5;
	//COLOR = texture(TEXTURE, (ball+vec2(1., 1.))*.5);
	//COLOR.rgb = vec3(ball);

}